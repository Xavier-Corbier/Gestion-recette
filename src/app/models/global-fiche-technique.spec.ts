import { GlobalFicheTechnique } from './global-fiche-technique';

describe('GlobalFicheTechnique', () => {
  it('should create an instance', () => {
    expect(new GlobalFicheTechnique()).toBeTruthy();
  });
});
